import { createStyles } from "@mantine/core";

export const useStyles = createStyles(() => ({
    
    container: {
        width: "50%",
        padding: "25%",
    },

    textBold: {
        fontWeight: 400,
    },

    listSize: {
        margin: "20px 0 20px;",
    },

    priceText: {
        marginTop: "10px",
        fontWeight: 400,
    },

    textDescription: {
        marginBottom: "10px",
    },
}));
